name: Validate templates

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]
  workflow_dispatch:

permissions:
  contents: read

jobs:
  validate:
    uses: colin-gourlay/todoist-playbook/.github/workflows/reusable-validate-templates.yml@main
